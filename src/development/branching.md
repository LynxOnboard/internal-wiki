# Branching

## Rules
- `main` is protected (auto deploys to vercel)
- Feature branches per task
- No direct commits to `main`
- `dev` is for staging

## Feature branch naming conventions
- `feature-{name}` for adding features
- `hotfix-{name}` for fixes or refractoring